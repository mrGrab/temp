let fs;_a83‍.x([["default",()=>_a83‍.o]]);_a83‍.w("fs",[["default",["fs"],function(v){fs=v}]]);

const callback = (resolve, reject) => (error, ...args) => {
  if (error) {
    reject(error);
  } else {
    resolve(args);
  }
};

_a83‍.d(Object.keys(fs).reduce((result, key) => {
  // eslint-disable-next-line no-param-reassign
  result[key] = (...args) =>
    new Promise((resolve, reject) => {
      fs[key](...args, callback(resolve, reject));
    });
  return result;
}, {}));
